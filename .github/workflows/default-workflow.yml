name: default
on: push
defaults:
  run: 
    shell: sh
    working-directory: scripts
jobs:
  example:
    name: ltk-gem-example-job
    runs-on: ubuntu-latest
    steps:
      - id: one
        run: ls
